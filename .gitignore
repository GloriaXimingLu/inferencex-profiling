# Upstream benchmark repo — clone it yourself (see RUNBOOK §1):
#   git clone https://github.com/SemiAnalysisAI/InferenceX
InferenceX/

# Python
.venv/
__pycache__/
*.pyc

# Local creds / keys — never commit
*.pem
*_creds.json
credentials.json

# OS
.DS_Store

# local provisioning copy with real SSH key
nebius/cloud-init.local.yaml

# bulky per-run raw experiment artifacts (keep .json/.tsv/.prom/.png, drop the rest)
nebius/results_exp/*.samples
nebius/results_exp/*.m0
nebius/results_exp/*.m1
nebius/results_exp/*.serverlog
nebius/results_exp/*.client.log
nebius/results_exp/*.log
nebius/results_exp/exp.out

# internal co-worker reports — DO NOT publish
*.pdf

# tau-bench bulky per-part artifacts (keep client.json + m0/m1 + table + png)
nebius/results_tau/*.samples
nebius/results_tau/*.client.err

# co-worker private repo (clone separately) + local creds
tau-bench-replay/
tau-bench-replay

# local provisioning copy with real infra IDs
nebius/provision_tau_node.local.sh

nebius/results_tau_c64/*.samples
nebius/results_tau_c64/*.client.err
